programme Test28 debut
    entier[2] tab;
    entier cpt;
    cpt = 0;
    tantque cpt < 10 repeter
        tab[0] = tab[0] + cpt;
        ecrire tab[0];
        cpt = cpt + 1;
    fintantque
    tab[1] = tab[0];
    ecrire tab[0];
    ecrire tab[1];
fin